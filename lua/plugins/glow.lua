return {
  "ellisonleao/glow.nvim",
  cmd = "Glow",
  ft = { "markdown" },
  opts = {
    style = "dark", -- Style of the preview (dark/light)
    width = 120,    -- Floating window width
    height = 100,   -- Floating window height
    width_ratio = 0.8, -- Width ratio of floating window
    height_ratio = 0.8, -- Height ratio of floating window
  },
}
